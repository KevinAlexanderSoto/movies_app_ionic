import { ArrayToArraydoblePipe } from './array-to-arraydoble.pipe';

describe('ArrayToArraydoblePipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayToArraydoblePipe();
    expect(pipe).toBeTruthy();
  });
});
